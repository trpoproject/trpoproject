#include <iostream>


using namespace std;

int main()
{

std:cout << "vvedite 3 storon treugolnika";
	int a, b, c;
	cin >> a >> b >> c;
	if (a + b > c && a + c > b && b + c > a)
		cout << "Da" << endl;
	else
		cout << "Net" << endl;
	
	return 0;
}
